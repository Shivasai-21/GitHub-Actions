name: Multiple jobs
on: 
  push:
    branches:
      - main
      - develop

  pull_request:
    branches:
      - main
    
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: build step
        run: echo "this is build stage"

