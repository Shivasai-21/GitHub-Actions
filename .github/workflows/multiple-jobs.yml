name: Multiple jobs
on: 
  push:
    branches:
      - main
      - develop

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: build step
        run: echo "this is ${{secrets.My_Secret}}"
